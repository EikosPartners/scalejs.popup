{
  "name": "scalejs.popup",
  "version": "0.0.1",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir dist && npm run copy",
    "copy": "copyup src/**/*.html src/**/*.css dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EikosPartners/scalejs.popup.git"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "copyfiles": "^0.2.1"
  },
  "main": "dist/scalejs.popup.js"
}
